source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

group :development do
  gem "irbtools", require: "irbtools/binding"
  gem "rubocop"
  gem "solargraph"
end

# Specify your gem's dependencies in gpodder.gemspec
gemspec
